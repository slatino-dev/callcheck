"""mockserver — minimal OpenAI-compatible HTTP server for callcheck CI testing.

Returns configurable well-formed and deliberately-malformed tool-call responses
so that the scoring pipeline can be exercised without a real model.
"""
